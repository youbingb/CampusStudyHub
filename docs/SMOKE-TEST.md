# Campus Study Hub —— 冒烟测试文档

> **目的**：每次发布前逐项验证系统核心链路，确保所有模块可用、Bug 已修复。
> **前置条件**：MySQL 8 已启动，数据库 `campus_study_hub` 已初始化（`schema.sql` + `data.sql`）。
> **测试账号**：
>
> | 角色 | 用户名 | 密码 | 说明 |
> |------|--------|------|------|
> | 管理员 | admin | 123456 | 系统管理员（ID=1） |
> | 管理员 | admin2 | 123456 | 巡检管理员（ID=2） |
> | 学生 | stu01 | 123456 | 张三，学号 20230001，信誉 100（ID=3） |
> | 学生 | stu02 | 123456 | 李四，学号 20230002，信誉 100（ID=4） |
> | 学生 | stu03 | 123456 | 王五，学号 20230003，信誉 95（ID=5） |
> | 学生 | stu04 | 123456 | 赵六，学号 20230004，信誉 100（ID=6） |
> | 学生 | stu05 | 123456 | 钱七，学号 20230005，信誉 80（ID=7） |
>
> **默认预约规则**（`reservation_rule` 表）：
> - 每日最多预约：2 次
> - 提前天数：3 天
> - 最低信誉：60 分
> - 签到宽限期：15 分钟
> - 最长时长：4 小时
> - 爽约扣分：5 分
>
> **测试数据**：3 间自习室 × 30 座 = 90 座位，A27 为故障座。1 条已发布公告。
>
> **地址**：前端 `http://localhost:5173`，后端 `http://localhost:8080`

---

## A. 环境启动与健康检查

| # | 操作 | 预期结果 | 验证方式 |
|---|------|----------|----------|
| A.1 | 启动后端：`cd backend && mvn spring-boot:run` | 控制台输出 `Started CampusStudyHubApplication in X seconds`，无 ERROR 日志 | 观察控制台 |
| A.2 | 启动前端：`cd frontend && npm run dev` | 输出 `Local: http://localhost:5173/` | 观察控制台 |
| A.3 | 浏览器访问 `http://localhost:5173` | 自动 302 到 `/auth/login` | 地址栏 |
| A.4 | 访问 `http://localhost:8080/swagger-ui/index.html` | Knife4j/Swagger 文档页面正常渲染 | 页面内容 |
| A.5 | 访问 `http://localhost:5173/any-nonexist-path` | 显示 404 页面，有"返回首页"链接 | 页面内容 |

---

## B. 注册与登录（Phase 1）

### B.1 登录页渲染

| # | 操作 | 预期结果 |
|---|------|----------|
| B.1.1 | 访问 `/auth/login` | 页面居中显示登录卡片：顶部"书"字 logo + "校园自习室 Study Hub"，提示"学生与管理员统一登录入口"，用户名输入框（placeholder="用户名"），密码输入框（placeholder="密码"，右侧有眼睛图标可切换明文），"登 录"按钮（全宽，绿色），底部"还没有账号？立即注册"链接 |
| B.1.2 | 检查密码输入框右侧"眼睛"图标 | 点击可切换密码明文/密文显示 |

### B.2 登录表单校验

| # | 操作 | 预期结果 |
|---|------|----------|
| B.2.1 | 不输入任何内容，直接点击"登 录" | 用户名输入框下方红色提示"请输入用户名"，密码输入框下方红色提示"请输入密码" |
| B.2.2 | 只输入用户名，不输密码，点击登录 | 密码框下方提示"请输入密码" |
| B.2.3 | 只输密码，不输用户名，点击登录 | 用户名框下方提示"请输入用户名" |

### B.3 登录业务逻辑

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| B.3.1 | 输入 `admin / wrongpwd`，点击登录 | 页面顶部弹出错误提示"用户名或密码错误"（ElMessage.error） | - |
| B.3.2 | 输入 `stu01 / 123456`，点击登录 | ① 顶部弹出绿色"登录成功"提示 ② 自动跳转到 `/student/home` ③ 侧栏底部显示"张三"和信誉分 | - |
| B.3.3 | 退出后输入 `admin / 123456`，点击登录 | ① 提示"登录成功" ② 自动跳转到 `/admin/dashboard` ③ 侧栏显示管理员专属菜单（控制台、自习室管理、座位管理、预约订单、举报处理、用户管理、巡检记录、公告管理、预约规则、数据统计、操作日志） | - |
| B.3.4 | 禁用 stu05（数据库 `UPDATE sys_user SET status=0 WHERE id=7`），用 `stu05 / 123456` 登录 | 提示"账号已被禁用"，无法登录 | - |
| B.3.5 | 恢复 stu05（`UPDATE sys_user SET status=1 WHERE id=7`） | - | `status=1` |

### B.4 登录后状态保持

| # | 操作 | 预期结果 |
|---|------|----------|
| B.4.1 | 登录后按 F5 刷新页面 | 仍然保持登录态，用户信息正确显示，不跳转到登录页 |
| B.4.2 | 登录后关闭浏览器标签，重新打开同一地址 | 仍然保持登录态（token 在 localStorage `csh-token` 中） |
| B.4.3 | 手动清除 localStorage 中的 `csh-token`，刷新页面 | 自动跳转到 `/auth/login` |

### B.5 路由守卫

| # | 操作 | 预期结果 |
|---|------|----------|
| B.5.1 | 未登录状态下直接访问 `/student/home` | 自动跳转到 `/auth/login?redirect=%2Fstudent%2Fhome` |
| B.5.2 | 未登录状态下直接访问 `/admin/dashboard` | 自动跳转到 `/auth/login` |
| B.5.3 | 学生（stu01）登录后直接访问 `/admin/dashboard` | 自动跳转回 `/student/home`（无 ADMIN 权限） |
| B.5.4 | 管理员（admin）登录后访问 `/student/home` | 正常显示学生首页（管理员也有 STUDENT 权限访问学生页面） |

### B.6 注册

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| B.6.1 | 在登录页点击"立即注册" | 跳转到 `/auth/register`，显示注册卡片：logo + "注册账号"标题，表单含用户名、密码、姓名、学号、手机、邮箱，底部"注 册"按钮，"已有账号？去登录"链接 | - |
| B.6.2 | 用户名留空，点击"注 册" | 用户名下方提示"请输入用户名" | - |
| B.6.3 | 密码输入 `123`（不足 6 位），点击注册 | 密码下方提示"密码至少 6 位" | - |
| B.6.4 | 填写 `username=newstu, password=123456, realName=测试, studentNo=20230099, phone=13900000099, email=test@csh.edu`，点击注册 | ① 提示"注册成功，请登录" ② 自动跳转到 `/auth/login` | `sys_user` 新增一条，role=STUDENT, credit_score=100, status=1 |
| B.6.5 | 用刚注册的 `newstu / 123456` 登录 | 登录成功，跳转到 `/student/home`，侧栏显示"测试" | - |
| B.6.6 | 再次用相同用户名 `newstu` 注册 | 提示"用户名已存在"（code=400） | - |
| B.6.7 | 用已存在的学号 `20230001` 注册（不同用户名） | 提示"学号已存在"（code=400） | - |

### B.7 退出登录

| # | 操作 | 预期结果 |
|---|------|----------|
| B.7.1 | 登录后点击右上角用户下拉菜单 → "退出登录" | 清除 localStorage token，跳转到 `/auth/login` |
| B.7.2 | 退出后按浏览器"后退"按钮 | 不能回到已登录的页面，仍停留在登录页或被守卫拦截 |

---

## C. 学生端 — 首页（`/student/home`）

| # | 操作 | 预期结果 |
|---|------|----------|
| C.1 | 学生登录后查看首页 | 顶部问候语"你好，张三" + 副标题"欢迎回到校园自习室，祝你学习愉快" |
| C.2 | 查看快捷入口区域 | 6 个卡片，2×3 网格布局：自习室、我的预约、智能推荐、消息通知、违规举报、个人中心，每个有图标和文字 |
| C.3 | 点击"自习室"快捷卡片 | 跳转到 `/student/rooms` |
| C.4 | 点击"我的预约"快捷卡片 | 跳转到 `/student/reservations` |
| C.5 | 点击"智能推荐"快捷卡片 | 跳转到 `/student/recommend` |
| C.6 | 点击"消息通知"快捷卡片 | 跳转到 `/student/notifications` |
| C.7 | 点击"违规举报"快捷卡片 | 跳转到 `/student/reports` |
| C.8 | 点击"个人中心"快捷卡片 | 跳转到 `/student/profile` |
| C.9 | 查看"最新公告"区域 | 显示已发布公告（初始数据有 1 条"系统试运行公告"），含标题、内容摘要、发布时间；多条时自动轮播（5 秒间隔） |
| C.10 | 鼠标悬停快捷卡片 | 卡片有上浮阴影动画效果 |

---

## D. 学生端 — 自习室列表（`/student/rooms`）

| # | 操作 | 预期结果 |
|---|------|----------|
| D.1 | 点击侧栏"自习室" | 页面标题"自习室"，显示所有开放中的自习室卡片列表 |
| D.2 | 查看自习室卡片信息 | 每张卡片显示：名称（如"A101 安静自习室"）、位置（如"图书馆 1 楼东侧"）、开放时间（如"07:00–22:30"）、可用座位数（如"29/30"）、描述 |
| D.3 | 确认只显示开放的自习室 | `status=0`（关闭）的自习室不出现在列表中 |
| D.4 | 点击某自习室的"进入选座"按钮 | 跳转到 `/student/rooms/{id}` 座位平面图 |

---

## E. 学生端 — 座位平面图（`/student/rooms/:id`）

### E.1 页面渲染

| # | 操作 | 预期结果 |
|---|------|----------|
| E.1.1 | 进入 A101 安静自习室（room_id=1） | 顶部：返回按钮 + "A101 安静自习室"标题 + 位置/时间信息；中部：图例（绿色=空闲 N、橙色=已预约 N、灰色=使用中 N、红色=故障 N）+ "📺 屏幕方向"标记 |
| E.1.2 | 查看座位网格 | 5 行 × 6 列网格，每个格子 36×36px，圆角，内显示座位号（去掉前缀，如"A1"显示"1"） |
| E.1.3 | 确认 A27 显示为红色（故障） | A27 格子为红色背景，且 `disabled` 不可点击 |
| E.1.4 | 确认其他座位显示为绿色（空闲） | 除 A27 外所有座位为绿色背景 |
| E.1.5 | 查看图例统计数字 | 空闲=29，已预约=0，使用中=0，故障=1（与实际座位状态一致） |

### E.2 座位抽屉

| # | 操作 | 预期结果 |
|---|------|----------|
| E.2.1 | 点击座位 A15（空闲） | 底部弹出抽屉（direction=btt, 56% 高度），标题"A15" |
| E.2.2 | 查看抽屉内容 | 座位编号"A15"、位置"第 3 排 / 第 3 列"、状态标签"空闲"（绿色）、特性标签（如"安静区"）、开始时间选择器（默认当前时间 +5 分钟）、结束时间选择器（默认当前时间 +2 小时） |
| E.2.3 | 点击抽屉右上角关闭按钮 | 抽屉关闭 |
| E.2.4 | 点击故障座位 A27 | 无反应（disabled 状态） |

### E.3 预约流程

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| E.3.1 | 点击座位 A5 → 修改结束时间为开始时间之前 → 点击"提交预约" | 提示"结束时间必须晚于开始时间" | 无新记录 |
| E.3.2 | 点击座位 A5 → 保持默认时间 → 点击"提交预约" | ① 提示"预约成功" ② 抽屉关闭 ③ 自动跳转到 `/student/reservations` | `reservation` 表新增一条：user_id=3, seat_id=5, status=BOOKED |
| E.3.3 | 返回座位图查看 A5 | A5 变为橙色（已预约），图例"已预约"计数 +1 | seat 表 seat_id=5 status 变为 RESERVED |
| E.3.4 | 用 stu02 登录，对同一座位相同时段预约 | 提示"该时段已被预约"（code=4001） | 无新记录 |
| E.3.5 | 预约一个已故障的座位（如直接调 API） | 提示"座位处于故障状态，无法预约"（code=4000） | 无新记录 |

### E.4 座位实时推送（WebSocket）

| # | 操作 | 预期结果 |
|---|------|----------|
| E.4.1 | 浏览器 A（stu01）和浏览器 B（stu02）同时打开 A101 座位图 | 两边都显示相同的座位颜色 |
| E.4.2 | A 预约座位 A10 | B 的座位图上 A10 实时变为橙色（无需手动刷新），图例数字同步更新 |
| E.4.3 | A 取消预约 A10 | B 的座位图上 A10 实时恢复绿色 |

---

## F. 学生端 — 我的预约（`/student/reservations`）

### F.1 列表与 Tab

| # | 操作 | 预期结果 |
|---|------|----------|
| F.1.1 | 点击侧栏"我的预约" | 标题"我的预约"，下方 3 个 Tab：进行中（默认选中）、历史、全部 |
| F.1.2 | "进行中" Tab | 只显示 status=BOOKED 或 CHECKED_IN 的预约 |
| F.1.3 | "历史" Tab | 只显示 status=COMPLETED、CANCELLED、EXPIRED 的预约 |
| F.1.4 | "全部" Tab | 显示所有状态的预约 |
| F.1.5 | 查看预约卡片内容 | 每条显示：房间名 · 座位号（如"A101 安静自习室 · A5"）、状态标签（待签到=warning/使用中=success/已完成=info/已取消=info/超时=danger）、时段（开始~结束）、签到时间（如有）、签退时间（如有） |
| F.1.6 | 无预约时 | 显示空状态"暂无预约" |

### F.2 签到

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| F.2.1 | 对一条"待签到"预约点击"签到"按钮 | 弹出确认框"确定签到 A101 安静自习室 · A5？" | - |
| F.2.2 | 点击"取消" | 关闭确认框，无变化 | - |
| F.2.3 | 点击"确定" | ① 提示"签到成功" ② 列表刷新，该预约状态变为"使用中"（绿色标签） ③ 显示签到时间 | reservation.status=CHECKED_IN, check_in_time=当前时间 |
| F.2.4 | **回到座位图查看对应座位** | **该座位变为灰色（使用中），图例"使用中"计数 +1**（Bug #2 修复验证核心点） | seat.status=OCCUPIED |
| F.2.5 | 签到时间早于预约开始时间（宽限期内） | 签到成功，座位仍正确变为灰色 | 同上，验证 `startTime<=now` 条件已去除 |

### F.3 签退

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| F.3.1 | 对一条"使用中"预约点击"签退"按钮 | 弹出确认框"确认提前签退 A101 安静自习室 · A5？" | - |
| F.3.2 | 确认签退 | ① 提示"已签退" ② 状态变为"已完成" ③ 显示签退时间 | status=COMPLETED, check_out_time=当前时间 |
| F.3.3 | 回到座位图 | 该座位恢复绿色（空闲） | seat.status=AVAILABLE |

### F.4 取消预约

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| F.4.1 | 对一条"待签到"预约点击"取消"按钮 | 弹出确认框"确认取消该预约？" | - |
| F.4.2 | 确认取消 | ① 提示"已取消" ② 状态变为"已取消" ③ 座位图上该座位恢复绿色 | status=CANCELLED, seat.status 重算 |
| F.4.3 | 对"使用中"预约点击"取消" | 无"取消"按钮（只有"签退"按钮） | - |
| F.4.4 | 对"已完成"/"已取消"预约 | 无操作按钮 | - |

---

## G. 学生端 — 站内消息（`/student/notifications`）

### G.1 列表渲染

| # | 操作 | 预期结果 |
|---|------|----------|
| G.1.1 | 点击侧栏"消息" | 标题"站内消息"，右侧红色角标"N 条未读"，下方筛选栏 + 消息列表 |
| G.1.2 | 查看筛选栏 | 左侧：全部/未读/已读 三个 radio button（"全部"默认选中，深绿色背景）；中间："按类型筛选"下拉框（可选：预约创建/预约取消/预约超时/预约提醒/举报受理/举报处理/信誉变更/系统公告/系统消息）；**右侧：深绿色"全部已读"按钮**（Bug #1 修复验证点） |
| G.1.3 | 查看消息卡片 | 每条显示：类型标签（颜色区分：success/warning/info/danger）、标题、内容、时间（MM-DD HH:mm 格式） |
| G.1.4 | 未读消息样式 | 浅绿色背景（`--accent-soft: #e8efe3`），标题加粗 |
| G.1.5 | 已读消息样式 | 白色背景，标题正常粗细 |
| G.1.6 | 无消息时 | 显示"暂无消息" |

### G.2 筛选

| # | 操作 | 预期结果 |
|---|------|----------|
| G.2.1 | 点击"未读" Tab | 列表只显示 read_flag=0 的消息 |
| G.2.2 | 点击"已读" Tab | 列表只显示 read_flag=1 的消息 |
| G.2.3 | 点击"全部" Tab | 显示所有消息 |
| G.2.4 | 选择"按类型筛选"→"预约创建" | 列表只显示 type=RESERVATION_CREATED 的消息 |
| G.2.5 | 清除类型筛选 | 恢复显示所有消息 |

### G.3 已读操作

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| G.3.1 | 点击一条未读消息 | ① 消息背景变为白色 ② 标题取消加粗 ③ 侧栏"消息"旁的未读数角标 -1 | notification.read_flag=1 |
| G.3.2 | 点击一条已读消息 | 无变化（不会重复操作） | - |
| G.3.3 | 有未读消息时，确认"全部已读"按钮可见且可点击 | 按钮为深绿色，白色文字"全部已读"，非 disabled 状态 | - |
| G.3.4 | 无未读消息时 | "全部已读"按钮为 disabled 灰色状态 | - |
| G.3.5 | 点击"全部已读" | 弹出确认框"确认将所有未读通知标记为已读?" | - |
| G.3.6 | 确认 | ① 提示"已标记 N 条" ② 所有未读消息变为已读 ③ 侧栏角标消失 | 所有 read_flag=0 的记录变为 1 |

### G.4 分页

| # | 操作 | 预期结果 |
|---|------|----------|
| G.4.1 | 消息超过 20 条时 | 底部显示分页器（上一页/页码/下一页/总数） |
| G.4.2 | 翻到第 2 页 | 显示第 2 页数据，页码高亮 |

### G.5 WebSocket 实时推送

| # | 操作 | 预期结果 |
|---|------|----------|
| G.5.1 | 管理员调整学生信誉分后 | 学生侧栏"消息"旁角标实时 +1（无需刷新） |
| G.5.2 | 预约超时后 | 学生收到"预约超时"通知，角标 +1 |

---

## H. 学生端 — 智能推荐（`/student/recommend`）

| # | 操作 | 预期结果 |
|---|------|----------|
| H.1 | 点击侧栏"推荐" | 标题"推荐"，显示时间选择器和推荐结果列表 |
| H.2 | 选择开始/结束时间后查询 | 显示推荐座位列表，按综合评分降序排列 |
| H.3 | 查看推荐卡片 | 每条显示：座位编号、房间名、推荐分数（0~1）、分项得分（房间偏好/特性偏好/邻座空闲/常坐/冲突）、推荐理由标签（如"常去 主楼101""邻座清净""有插座""靠窗"） |
| H.4 | 首次使用（无历史数据） | 仍能推荐，分数主要由邻座空闲度和冲突度决定 |
| H.5 | 限定房间查询 | 只推荐指定房间的座位 |

---

## I. 学生端 — 我的举报（`/student/reports`）

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| I.1 | 点击侧栏"我的举报" | 标题"我的举报"，显示举报记录列表 | - |
| I.2 | 点击"新建举报"（如有此按钮） | 弹出表单：类型下拉（占座/喧哗/设施损坏/其他）、描述文本框、可选：目标用户ID/关联预约ID/座位ID/证据URL | - |
| I.3 | 填写类型="占座"，描述="A5 座位长时间无人但物品占座"，提交 | ① 提示提交成功 ② 列表中出现新记录，状态为"待处理" | report 表新增一条，status=PENDING |
| I.4 | 对"待处理"举报点击"撤销" | 记录被移除或状态变更 | status 变更或 deleted=1 |
| I.5 | 对"已处理"举报 | 无"撤销"按钮，显示处理结果 | - |

---

## J. 学生端 — 个人中心（`/student/profile`）

### J.1 基本信息

| # | 操作 | 预期结果 |
|---|------|----------|
| J.1.1 | 点击侧栏"个人中心" | 标题"个人中心"，下方 3 个卡片 |
| J.1.2 | "基本信息"卡片 | 显示：用户名（只读）、学号（只读）、角色标签（学生=info/管理员=danger）、信誉分标签（≥80=success/<80=warning） |

### J.2 编辑资料

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| J.2.1 | "编辑资料"卡片 | 表单含：姓名、手机、邮箱，底部"保存资料"按钮 | - |
| J.2.2 | 手机输入 `123`（格式错误），点击保存 | 手机下方提示"手机号格式错误" | - |
| J.2.3 | 邮箱输入 `abc`（格式错误），点击保存 | 邮箱下方提示"邮箱格式错误" | - |
| J.2.4 | 修改姓名为"张三丰"，点击保存 | ① 提示"资料已更新" ② 页面信息更新 | real_name="张三丰" |
| J.2.5 | 刷新页面 | 姓名仍为"张三丰"（已持久化） | - |

### J.3 修改密码

| # | 操作 | 预期结果 |
|---|------|----------|
| J.3.1 | "修改密码"卡片 | 表单含：旧密码、新密码、确认密码（均带眼睛图标），底部"提交修改"按钮 |
| J.3.2 | 新密码输入 `123`（不足 6 位） | 提示"密码长度 6-64" |
| J.3.3 | 新密码和确认密码不一致 | 提示"两次输入不一致" |
| J.3.4 | 旧密码错误 | 提示"旧密码错误" |
| J.3.5 | 正确填写旧密码 + 新密码 + 确认密码，提交 | ① 提示"密码已更新，请重新登录" ② 自动退出登录 ③ 跳转到登录页 |
| J.3.6 | 用新密码重新登录 | 登录成功 | password 已更新 |

---

## K. 管理端 — 控制台（`/admin/dashboard`）

| # | 操作 | 预期结果 |
|---|------|----------|
| K.1 | 管理员登录后查看控制台 | 标题"控制台"，显示数据概览（用户总数、今日预约、活跃自习室等统计卡片） |
| K.2 | 查看图表区域 | 包含 ECharts 图表（自习室使用率、热门时段等），图表正常渲染无白屏 |
| K.3 | 侧栏导航完整 | 11 个菜单项全部可见：控制台、自习室管理、座位管理、预约订单、举报处理、用户管理、巡检记录、公告管理、预约规则、数据统计、操作日志 |

---

## L. 管理端 — 自习室管理（`/admin/rooms`）

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| L.1 | 进入"自习室管理" | 显示全部自习室列表（含已关闭），表格含：名称、位置、容量、开放时间、状态、操作 | - |
| L.2 | 点击"新增"按钮 | 弹出表单：名称（必填，唯一）、位置、容量（≥0）、开放时间（默认 07:00）、关闭时间（默认 22:30）、描述 | - |
| L.3 | 填写名称="A201 测试室"，提交 | ① 提示成功 ② 列表中出现新自习室 | study_room 新增一条 |
| L.4 | 编辑"A201 测试室"，修改位置为"教学楼 2 层" | 列表中位置更新 | location 更新 |
| L.5 | 将某自习室 status 改为"关闭" | 学生端自习室列表不再显示该房间 | status=0 |
| L.6 | 删除某自习室 | 软删除（deleted=1），列表不再显示 | deleted=1 |

---

## M. 管理端 — 座位管理（`/admin/seats`）

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| M.1 | 进入"座位管理"，选择自习室 | 显示该房间所有座位列表，含：座位编号、行、列、状态、特性 | - |
| M.2 | 点击"批量生成"，输入 rows=5, cols=6, prefix="D" | 新座位出现在列表中，编号格式 D1-1 ~ D5-6 | seat 表新增 30 条 |
| M.3 | 新增单个座位 | 填写 roomId/seatNo/rowNo/colNo/feature 后提交成功 | seat 新增一条 |
| M.4 | 对 A27 点击"标记故障"（输入原因） | ① A27 状态变为"故障" ② 学生端座位图 A27 变红 | seat_fault 新增 OPEN 记录，seat.status=FAULT |
| M.5 | 对 A27 点击"解除故障" | ① 状态恢复（根据当前预约重算） ② 学生端座位图同步更新 | seat_fault.status=FIXED |
| M.6 | 点击"刷新状态" | 座位状态按当前 reservation 时段重新计算 | seat.status 重算 |

---

## N. 管理端 — 预约订单（`/admin/reservations`）

| # | 操作 | 预期结果 |
|---|------|----------|
| N.1 | 进入"预约订单" | 分页显示所有预约，含：用户、座位、时段、状态、操作 |
| N.2 | 按状态筛选（如只看 BOOKED） | 列表只显示对应状态的预约 |
| N.3 | 按用户 ID 筛选 | 列表只显示该用户的预约 |
| N.4 | 按房间筛选 | 列表只显示该房间的预约 |
| N.5 | 对一条 BOOKED 预约点击"取消" | 输入取消原因 → 确认后：① 状态变为"已取消" ② 用户收到"预约被管理员取消"通知 | 座位状态自动刷新 |

---

## O. 管理端 — 用户管理（`/admin/users`）

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| O.1 | 进入"用户管理" | 分页显示所有用户，含：用户名、姓名、学号、角色、信誉分、状态、操作 | - |
| O.2 | 搜索框输入"张三" | 列表按用户名/姓名/学号/手机号模糊匹配 | - |
| O.3 | 搜索框输入"20230001" | 匹配到 stu01（学号匹配） | - |
| O.4 | 将 stu05 的状态切换为"禁用" | ① 状态变为禁用 ② stu05 无法登录（提示"账号已被禁用"） | status=0 |
| O.5 | 将 stu05 恢复"启用" | stu05 可正常登录 | status=1 |
| O.6 | 对 stu05（信誉 80）点击"调整信誉"，输入 delta=-10, reason="测试扣分" | ① 信誉变为 70 ② stu05 收到"信誉变更"通知 ③ 信誉分标签变为 warning 色 | credit_score=70, credit_log 新增一条, notification 新增一条 CREDIT_CHANGED |
| O.7 | 对 stu05 输入 delta=+20, reason="测试加分" | 信誉变为 90 | 同上 |

---

## P. 管理端 — 举报处理（`/admin/reports`）

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| P.1 | 进入"举报处理" | 分页显示所有举报，含：类型、描述、举报人、目标用户、状态、操作 | - |
| P.2 | 按状态筛选 PENDING | 只显示待处理举报 | - |
| P.3 | 查看举报详情 | 显示完整信息：类型、描述、证据 URL、关联预约/座位 | - |
| P.4 | 处理举报：action=APPROVE, result="核实通过", creditDelta=-5 | ① 状态变为"已处理" ② 被举报人信誉 -5 ③ 被举报人收到"信誉变更"通知 ④ 操作日志记录 | report.status=RESOLVED, credit_log 新增 |
| P.5 | 处理举报：action=REJECT, result="证据不足" | 状态变为"已驳回"，无信誉变更 | report.status=REJECTED |

---

## Q. 管理端 — 巡检记录（`/admin/inspections`）

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| Q.1 | 进入"巡检记录" | 分页显示巡检记录，含：房间、巡检人、内容、问题座位、时间 | - |
| Q.2 | 新增巡检：选择房间=A101, content="常规巡检", issues=[A10] | ① 新记录出现 ② A10 自动标记为故障 ③ 学生端座位图 A10 变红 ④ 操作日志记录 | inspection 新增, seat_fault 新增, seat.status=FAULT |
| Q.3 | 删除一条巡检记录 | 记录消失，操作日志记录 | - |

---

## R. 管理端 — 公告管理（`/admin/announcements`）

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| R.1 | 进入"公告管理" | 分页显示公告列表（含草稿），含：标题、状态（已发布/草稿）、发布时间、操作 | - |
| R.2 | 新增公告：标题="维护通知", content="系统将于今晚 22:00 维护", publishNow=true | ① 列表中出现新公告，状态"已发布" ② 学生端首页"最新公告"可见 ③ 操作日志记录 | status=1, published_at=now |
| R.3 | 新增公告：publishNow=false | 状态为"草稿"，学生端不可见 | status=0 |
| R.4 | 对草稿点击"发布" | 状态变为"已发布"，学生端可见 | status=1 |
| R.5 | 对已发布公告点击"下架" | 状态变为"草稿"，学生端不再可见 | status=0 |
| R.6 | 编辑公告标题/内容 | 更新成功 | - |
| R.7 | 删除公告 | 软删除，列表不再显示 | deleted=1 |

---

## S. 管理端 — 预约规则（`/admin/rules`）

| # | 操作 | 预期结果 | 数据库验证 |
|---|------|----------|------------|
| S.1 | 进入"预约规则" | 显示当前 6 项规则及其值：每日上限(2)、提前天数(3)、最低信誉(60)、签到宽限期(15min)、最长时长(4h)、爽约扣分(5) | - |
| S.2 | 修改"每日上限"为 3，保存 | ① 提示保存成功 ② 页面显示新值 ③ 操作日志记录 | max_daily=3 |
| S.3 | 学生端预约验证新规则 | 学生一天内可预约 3 次（之前只能 2 次） | - |
| S.4 | 修改"最低信誉"为 70，保存 | 信誉 65 的用户无法预约（提示"信誉分不足"） | min_credit=70 |

---

## T. 管理端 — 数据统计（`/admin/stats`）

| # | 操作 | 预期结果 |
|---|------|----------|
| T.1 | 进入"数据统计" | 显示 5 个维度的图表区域 |
| T.2 | 自习室使用率图表 | 按房间显示预约数/完成率/总座位时，柱状图或饼图正常渲染 |
| T.3 | 用户使用排行 | 按预约次数/完成次数/爽约次数/总时长排行，表格或图表 |
| T.4 | 热门时段 | 按 0~23 小时显示预约数量分布，折线图 |
| T.5 | 违规统计 | 显示用户信誉/违规次数/总扣分 |
| T.6 | 故障统计 | 按房间显示故障数/未修复数/最新故障时间 |
| T.7 | 时间范围筛选 | 选择 from/to 日期后，所有图表数据按范围更新 |
| T.8 | 点击"导出 Excel" | 浏览器下载 .xlsx 文件，包含 5 个 Sheet（对应 5 项统计），操作日志记录 |

---

## U. 管理端 — 操作日志（`/admin/logs`）

| # | 操作 | 预期结果 |
|---|------|----------|
| U.1 | 进入"操作日志" | 分页显示操作日志，含：操作人、模块、动作、目标ID、IP、时间 |
| U.2 | 按模块筛选 | 列表正确过滤 |
| U.3 | 按操作人筛选 | 列表正确过滤 |
| U.4 | 按时间范围筛选 | 列表正确过滤 |
| U.5 | 验证操作日志自动落库 | 在其他管理页面执行操作（如发布公告、处理举报、修改规则）后，操作日志页面出现对应记录 |

---

## V. 定时任务与预约生命周期

| # | 场景 | 操作步骤 | 预期结果 | 数据库验证 |
|---|------|----------|----------|------------|
| V.1 | 预约超时未签到 | ① 创建预约，startTime=当前时间 ② 等待超过 grace_min(15分钟) + startTime | ① 预约自动变为 EXPIRED ② 用户收到"预约超时"通知 ③ 信誉扣 5 分 ④ 座位恢复 AVAILABLE | reservation.status=EXPIRED, credit_log -5, notification RESERVATION_EXPIRED |
| V.2 | 签到后自动签退 | ① 签到 ② 等待 endTime 到达 | ① 预约自动变为 COMPLETED ② check_out_time=endTime ③ 座位恢复 AVAILABLE | status=COMPLETED, seat.status=AVAILABLE |
| V.3 | 宽限期内签到 | ① 创建预约，startTime=10分钟后 ② 立即签到 | 签到成功（在 grace_min 窗口内），座位变为 OCCUPIED | status=CHECKED_IN, seat.status=OCCUPIED |
| V.4 | 宽限期后签到 | ① 创建预约 ② 等待超过 startTime + grace_min | 签到失败（状态已变为 EXPIRED） | - |

---

## W. 权限与安全

| # | 场景 | 操作 | 预期结果 |
|---|------|------|----------|
| W.1 | 无 token 调用需认证的 API | 直接访问 `GET /api/rooms`（不带 Authorization） | 返回 401 |
| W.2 | 过期 token | 使用过期 token 访问 | 返回 401 |
| W.3 | 学生调用管理接口 | 学生 token 调 `GET /api/admin/users` | 返回 403 |
| W.4 | 篡改 token | 修改 JWT payload 中的 role 为 ADMIN | 签名校验失败，返回 401 |
| W.5 | 越权操作 | stu01 尝试取消 stu02 的预约 | 返回 403"只能取消自己的预约" |
| W.6 | 越权签到 | stu01 尝试签到 stu02 的预约 | 返回 403"只能为自己的预约签到" |

---

## X. Bug 修复专项回归

### Bug #1 — 全部已读按钮不显示

| # | 步骤 | 预期结果 | 通过 |
|---|------|----------|------|
| X.1.1 | 学生登录 → 点击侧栏"消息" | 进入站内消息页面 | ☐ |
| X.1.2 | 查看筛选栏右侧 | **显示深绿色"全部已读"按钮**，白色文字清晰可见，非 disabled | ☐ |
| X.1.3 | 点击"全部已读" | 弹出确认框 → 确认后所有未读标为已读 | ☐ |
| X.1.4 | 无未读消息时查看按钮 | 按钮为 disabled 灰色状态 | ☐ |

### Bug #2 — 签到后座位状态不更新

| # | 步骤 | 预期结果 | 通过 |
|---|------|----------|------|
| X.2.1 | 学生登录 → 自习室 → 选座 A29 → 提交预约 | "预约成功" | ☐ |
| X.2.2 | 前往"我的预约" → 签到 | "签到成功"，状态变为"使用中" | ☐ |
| X.2.3 | 回到 A101 座位图 | **A29 变为灰色（使用中）**，图例"使用中"=1 | ☐ |
| X.2.4 | 签退后查看座位图 | A29 恢复绿色，"使用中"=0，"空闲"=29 | ☐ |
| X.2.5 | 创建预约 startTime=5min 后 → 立即签到（宽限期内）→ 回座位图 | A29 变灰色（验证宽限期签到也能正确更新状态） | ☐ |

---

## Y. 完整业务链路端到端测试

### Y.1 学生完整自习流程

| # | 步骤 | 预期结果 |
|---|------|----------|
| Y.1.1 | stu01 登录 | 跳转首页 |
| Y.1.2 | 首页 → 自习室 → A101 → 选座 A10 → 提交预约 | 预约成功 |
| Y.1.3 | 我的预约 → 签到 | 签到成功，座位变灰 |
| Y.1.4 | 签退 | 签退成功，座位恢复绿色 |
| Y.1.5 | 查看消息 | 收到"预约创建"通知 |

### Y.2 管理员完整管理流程

| # | 步骤 | 预期结果 |
|---|------|----------|
| Y.2.1 | admin 登录 → 控制台 | 看到数据概览 |
| Y.2.2 | 新增自习室 A301 + 批量生成座位 | 自习室和座位创建成功 |
| Y.2.3 | 标记某座位故障 → 学生端验证变红 | 故障状态同步 |
| Y.2.4 | 发布公告 → 学生端首页可见 | 公告生效 |
| Y.2.5 | 修改预约规则 → 学生端验证 | 新规则生效 |
| Y.2.6 | 处理举报 + 调整信誉 → 学生收到通知 | 跨模块联动正常 |
| Y.2.7 | 查看统计图表 + 导出 Excel | 数据正确，文件下载成功 |
| Y.2.8 | 查看操作日志 | 上述所有操作均有记录 |

---

## 测试结果汇总

| 模块 | 用例数 | 通过 | 失败 | 跳过 | 备注 |
|------|--------|------|------|------|------|
| A. 环境启动 | 5 | | | | |
| B. 注册与登录 | 22 | | | | |
| C. 首页 | 10 | | | | |
| D. 自习室列表 | 4 | | | | |
| E. 座位平面图 | 14 | | | | |
| F. 我的预约 | 13 | | | | |
| G. 站内消息 | 17 | | | | |
| H. 智能推荐 | 5 | | | | |
| I. 我的举报 | 5 | | | | |
| J. 个人中心 | 10 | | | | |
| K. 控制台 | 3 | | | | |
| L. 自习室管理 | 6 | | | | |
| M. 座位管理 | 6 | | | | |
| N. 预约订单 | 5 | | | | |
| O. 用户管理 | 7 | | | | |
| P. 举报处理 | 5 | | | | |
| Q. 巡检记录 | 3 | | | | |
| R. 公告管理 | 7 | | | | |
| S. 预约规则 | 4 | | | | |
| T. 数据统计 | 8 | | | | |
| U. 操作日志 | 5 | | | | |
| V. 定时任务 | 4 | | | | |
| W. 权限与安全 | 6 | | | | |
| X. Bug 回归 | 9 | | | | |
| Y. 端到端 | 13 | | | | |
| **合计** | **186** | | | | |

---

> 文档版本：v2.0
> 最后更新：2026-05-29
> 测试人：____________　　日期：____________
> 审核人：____________　　日期：____________
